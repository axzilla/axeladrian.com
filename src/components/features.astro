---
import { Icon } from "astro-icon/components";
import ContentSection from "~/components/content-section.astro";
import type { FeatureItem } from "~/types";

const features: Array<FeatureItem> = [
  {
    title: "Multi-Plattform-Experte",
    description:
      "Full-Stack Web, native Mobile Apps und Desktop-Anwendungen: Maßgeschneiderte Lösungen für jede Plattform.",
    icon: "mdi:handshake",
  },
  {
    title: "Full-Cycle Entwickler",
    description:
      "Von der strategischen Planung über Entwicklung und Testing bis zum Deployment und darüber hinaus.",
    icon: "mdi:feather",
  },
];
---

<ContentSection title="Mein Handwerk" id="features">
  <Fragment slot="lead">
    Mit <span class="text-primary">Kreativität</span>
    und <span class="text-primary">Code</span>
    bringe ich deine Vision zum Leben.
  </Fragment>
  <ul class="grid max-w-4xl grid-cols-2">
    {
      features.map(({ title, description, icon }) => (
        <li class="flex flex-col items-center gap-4 border border-default bg-offset p-6">
          <div class="size-16 rounded-full border-2 border-current p-3">
            <Icon name={icon} class="size-full" />
          </div>
          <p class="text-center font-extrabold text-xl">{title}</p>
          <p class="text-center text-offset text-sm">{description}</p>
        </li>
      ))
    }
  </ul>
</ContentSection>
